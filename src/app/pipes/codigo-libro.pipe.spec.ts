import { CodigoLibroPipe } from './codigo-libro.pipe';

describe('CodigoLibroPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigoLibroPipe();
    expect(pipe).toBeTruthy();
  });
});
